# Only stable options — a nightly-only setting here would make `cargo fmt` fail
# for contributors on the pinned stable toolchain.
edition = "2024"
max_width = 100
newline_style = "Unix"
use_field_init_shorthand = true
